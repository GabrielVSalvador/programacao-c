#include <stdio.h>
#include <stdlib.h>

int buscar_elemento (int elemento, int v[], int tamanho)
{
    int i = 0
    for(int i = 0; i <= tamnho; i++)
    {
        if(v[i]==elemento)
            return vetor[i];
    }
}

int main()
{

    printf("Digite n: ");
    scanf("%d", &n);
    int tamanho = n;
    int v[n] =
    printf("Digite o elemento que deseja buscar no vetor:");
    scanf("%d", &elemento);

}
